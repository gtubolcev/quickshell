name: Build

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            cmake \
            ninja-build \
            pkg-config \
            qt6-base-dev \
            qt6-declarative-dev \
            qt6-shadertools-dev \
            qt6-svg-dev qt6-wayland-dev qt6-wayland-private-dev libcli11-dev \
            libwayland-dev \
            wayland-protocols \
            libpipewire-0.3-dev \
            spirv-tools libxkbcommon-dev libdrm-dev libgbm-dev libegl1-mesa-dev libglib2.0-dev libpolkit-agent-1-dev \

      - name: Configure
        run: cmake -B build -G Ninja -DCMAKE_PREFIX_PATH=/usr -DCMAKE_BUILD_TYPE=Release -DUSE_JEMALLOC=OFF

      - name: Build
        run: cmake --build build
